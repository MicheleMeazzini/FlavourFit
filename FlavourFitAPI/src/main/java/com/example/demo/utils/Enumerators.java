package com.example.demo.utils;

public class Enumerators{
    public enum UserError{
        MISSING_USERNAME, MISSING_EMAIL, MISSING_PASSWORD, DUPLICATE_EMAIL, DUPLICATE_USERNAME, NO_ERROR, GENERIC_ERROR
    }
}
